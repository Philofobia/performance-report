# Core runtime (pinned for reproducibility per SECURITY_PLAN.md §2.7)
# Versions marked "verified" match the project venv (pip freeze from Phase 0).
# Packages not yet installed are pinned to current stable releases (verify on install).
pydantic==2.13.4          # verified in venv
PyYAML==6.0.3             # verified in venv
python-dotenv==1.2.2      # verified in venv
numpy==2.5.1              # exact top-k cosine for RAG retrieval (store/vectordb.py);
                          # replaces chromadb - see PROJECT_SPEC.md 8, decision #6
google-genai==2.16.0      # sole LLM + embeddings client (litellm dropped: 19 CVEs
                          # at the pinned version, unused, and spec-optional)
jinja2==3.1.6             # verified in venv
matplotlib==3.11.1        # 3.10.4 was never published; broke CI install
                          # reportlab: removed in Phase 5. It was pinned as a PDF
                          # fallback and imported by nothing; PDFs come from Chromium
                          # print-to-PDF (report/render_pdf.py). Do not re-add without
                          # a caller - see docs/superpowers/specs/2026-08-02-phase-5-*
                          # typer: removed in Phase 5. Pinned in Phase 0 as the
                          # CLI library, but every entry point uses argparse
                          # (ingest.manual, ingest.automated, analysis, report)
                          # and nothing ever imported it. Do not re-add without
                          # a caller.
playwright==1.48.0        # verified in venv (chromium installed)

# Test extras
pytest==9.1.1             # verified in venv
pytest-asyncio==1.4.0     # verified in venv
pytest-mock==3.14.0       # verified in venv
pytest-cov==7.1.0         # verified in venv
