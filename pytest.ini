[pytest]
testpaths = tests
markers =
    e2e: end-to-end browser tests requiring Playwright Chromium and network access (skipped offline)
    asyncio: async test (pytest-asyncio)
filterwarnings =
    ignore::DeprecationWarning
